name: Build Android APK From ZIP

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show files
        run: |
          ls -la
          unzip -l "wardrobe-ai (9).zip" | head -100
